import React from 'react';

import './Button.scss';

const Button: React.FC = (): JSX.Element => (
  <button type="button" className="brk-btn">
    Задать вопрос
  </button>
);

export default Button;
